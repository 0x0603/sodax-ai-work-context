# intents-whitelabel — context

Upstream: `github.com/icon-project/intents-whitelabel`

## Overview

TODO: what this is, relationship to the SDK/backend, how it's run locally.

## Layout

- `issues/` — one folder per GitHub issue / task (`gh-<n>-<title>/` or
  `task-<title>/`), each with `issue.md` · `plan.md` · `process.md` ·
  `outcome.md`. Scaffold: `scripts/new-issue.sh intents-whitelabel <n> <title...>`.
- `decisions/` — repo-scoped ADRs (cross-cutting ones go in top-level `decisions/`).
