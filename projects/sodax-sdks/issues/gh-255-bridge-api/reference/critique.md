Verified the load-bearing facts. The plan is strong and well-grounded; the #210-dependency analysis, the Bridge-specific deltas (no intent struct, no solver/`intent_hash`, no hub short-circuit, no quote/deadline/limit-orders), and the submit-tx + shared-deadline fallback are all correct against the digests. I confirmed: `RelayExtraDataResponseV2` and `PacketDataV2` are exported from `backendApiV2.ts`; `backend/index.ts` → `types/index.ts` → `packages/sdk/src/index.ts:14 export * from '@sodax/types'` means adding `backendBridgeApiV2.ts` to the backend barrel auto-propagates the new V2 types to `@sodax/sdk`'s public surface (so dapp-kit/demo imports from `@sodax/sdk` work without an extra re-export step — the plan's reliance is valid).

Concrete gaps / errors, ranked:

1. **Unresolved demo↔endpoint coupling for the token list (moderate).** §3.4 `BridgeCard` firmly says "tokens via `useBridgeApiTokens`," but Open Question #4 leaves it undecided whether `/bridge/tokens` and `/bridge/bridgeable-amount` exist at all (bridgeability is computed from on-chain vault reads: `srcToken.vault.toLowerCase() === dstToken.vault.toLowerCase()`, per `BridgeService.isBridgeable`/`getBridgeableTokens`). If those reads stay client-side, the demo must instead use `sodax.config.getSupportedTokensPerChain()` + the existing `useGetBridgeableTokens`/`useGetBridgeableAmount`, and `SelectChain` must source chains from config rather than an API token-map — a ripple across `BridgeCard.tsx`, `SelectChain.tsx`, and the `bridgeApi` hook list. The plan should pick a default (recommend: keep tokens/limits client-side via existing `sodax.bridge.*` reads, since the SDK already does the vault math and #210's swaps moved them server-side only because the backend had the data) instead of leaving the demo recipe asserting an endpoint that may not exist.

2. **`GetBridgeTokensByChainResponseV2` declared as an empty `interface { }` (concrete error).** An interface cannot represent an array; it must be `export type GetBridgeTokensByChainResponseV2 = readonly BridgeTokenV2[];` (mirroring how the swaps token-by-chain response is an array, and how `GetBridgeTokensResponseV2` is a `Record`). As written the sketch would compile to `{}` (matches anything) and silently break the valibot fidelity-at-consumer guard.

3. **`PacketDataV2` referenced but not imported (concrete, minor).** §3.1 uses `packetData?: PacketDataV2` inside `BridgeSubmitTxStatusResultV2` but the import block only pulls `RawTxReturnType` and `RelayExtraDataResponseV2`. Add `import type { PacketDataV2 } from './backendApiV2.js';` (it is exported — confirmed at `backendApiV2.ts:595`).

4. **"Migration workflow (v1 → v2)" section is inapplicable to Bridge (minor).** §3.5 mirrors `swaps-api/SKILL.md` including a `## Migration workflow (v1 → v2)`. The swaps skill had a genuine v1→v2 story (a v1 swap submit-tx API existed). There is **no** v1 Bridge API — Bridge has only the on-chain `sodax.bridge.*` orchestration. The bridge `SKILL.md` should drop that section or reframe it as "migrating from on-chain `sodax.bridge.bridge()` to the API client + `useBackendSubmitTx`," not reuse a non-existent v1→v2 doc.

5. **Demo loses "maximum bridgeable / route-not-available" UX parity (minor).** The current `BridgeManager` surfaces `useGetBridgeableAmount` ("Maximum bridgeable: … deposit/withdraw limit") and gates the button on `sodax.bridge.isBridgeable`. The §3.4 `BridgeCard` recipe omits surfacing the limit and the route-availability gate. Add `useBridgeApiBridgeableAmount` (or the existing client-side `useGetBridgeableAmount` + `isBridgeable`) so the API demo isn't a UX regression.

Verification points (not necessarily wrong, but unconfirmed against #210 and worth checking before coding):

- **`RelayExtraDataResponseSchema` export.** §3.2.A imports it from `swapsApiSchemas.js` into `bridgeApiSchemas.ts`. The digests show it is *used* inside `makeCreateIntentResponseSchema` but don't confirm it's an `export const` (the swaps schema module is #210-only and not in this tree). If it isn't exported, either export it or re-declare a trivial `v.object({ address: v.string(), payload: v.string() })` in `bridgeApiSchemas.ts`.
- **Idempotency of bridge re-relay (functional blocker for turning the flag on).** Open Question #6 correctly flags this, but it should be elevated: the swap fallback's safety rests on a swap-specific idempotency proof (`e2e-relay.test.ts`). Until bridge re-deposit/re-relay idempotency is independently verified, `bridgeOptions.useBackendSubmitTx` must ship default-off and should not be enabled in any demo/e2e path.

Minor improvements / nits:
- §3.2.B `submitTx`: confirm the locally-built `baseCtx = { srcChainKey, dstChainKey }` matches the field set the existing `bridge()` `baseCtx` passes to `executionFailed` (the current outer catch already uses a `baseCtx`); reuse the same shape to keep error context uniform.
- Hook/method naming: `useBridgeApiCreateIntent` calls `sodax.api.bridge.createBridgeIntent` — fine, but consider `useBridgeApiCreateBridgeIntent` (or rename the API method `createIntent`) so the hook name and method name line up the way `useSwapsApiCreateIntent`→`createIntent` did.
- `SubmitBridgeTxStatusV2` dropping `'posting_execution'` is speculative (Open Q#5); keep the schema picklist and the type in lockstep, and keep the status hook's inline terminal check (`'executed'|'failed'`) regardless so it's robust to whichever lifecycle the backend returns.
- The plan correctly says no `errors/codes.ts` change and no new wallet-provider methods — both verified-consistent with the digests; no action needed.

No invented APIs beyond what the plan already labels as unconfirmed (`getTokens`/`getBridgeableAmount`/gas/fees), and the #210 sequencing/prerequisite handling is correct.